import Loader from "@/components/misc/loader.component";

const Loading = () => {
  return (
    <div className="loadingPage">
      <Loader size="150px" />
    </div>
  );
};

export default Loading;
