export enum ButtonStyle {
  SOLID = "solid",
  OUTLINE = "outline",
  BACKDROP = "backdrop",
}

export enum ButtonColor {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  TEXT = "text",
  NEGATIVE = "negative",
}

export enum ButtonSize {
  SMALL = "small",
  NORMAL = "normal",
  LARGE = "large",
}
